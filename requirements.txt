# ─────────────────────────────────────────────────────────────────────────────
# pip requirements — mirrors the pip section of environment.yml.
# For Conda users, environment.yml is the source of truth.
# For pip-only users: pip install -r requirements.txt
#
# All versions match the pip freeze confirmed in requirements-lock.txt
# except pdfplumber / langdetect / rapidfuzz which were absent from the lock
# (added after the last freeze). Pin those three after your first clean install.
# ─────────────────────────────────────────────────────────────────────────────

# Numerical base (indirect deps — pinned to match Conda environment)
numpy==1.26.4
scipy==1.11.4
pandas==2.1.4
scikit-learn==1.3.2

# Embeddings / NLP
sentence-transformers==2.2.2
transformers==4.36.2
huggingface_hub==0.36.2
tokenizers==0.15.2          # upper-bounded by transformers 4.36.x (<0.19); explicit pin prevents drift
torch==2.11.0               # see note in environment.yml re: unusual version number

# Vector DB
chromadb==0.4.22            # DO NOT upgrade — SQLite schema break

# App / UI
streamlit==1.32.2

# LLM API — v2 only; v1 has breaking API differences
openai==2.32.0

# PDF / text processing
pdfplumber==0.11.0
langdetect==1.0.9
rapidfuzz==3.6.1

# Utilities
python-dotenv==1.2.2
requests==2.33.1
